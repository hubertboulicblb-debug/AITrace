% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observation.R
\name{assess_drift}
\alias{assess_drift}
\title{Assess drift between a baseline and a current observation}
\usage{
assess_drift(
  metric,
  baseline,
  observed,
  tolerance = NULL,
  direction = "MAINTAIN",
  unit = NULL,
  observation_id = NULL,
  monitoring_id = NULL,
  note = NULL,
  id = NULL,
  owner = NULL,
  metadata = list(),
  provenance = list()
)
}
\arguments{
\item{metric}{Metric name (character scalar), e.g. "AUROC" or "latency_p99".}

\item{baseline}{Baseline value (numeric or character scalar).}

\item{observed}{Observed value (numeric or character scalar).}

\item{tolerance}{Optional non-negative numeric drift tolerance.}

\item{direction}{One of \link{EFFECT_DIRECTION_LEVELS} describing which side of
the baseline counts as drift (see Details). Does not affect the
INCONCLUSIVE/NO_DRIFT cases, only which side of a tolerance breach is
flagged as DRIFT_DETECTED.}

\item{unit}{Optional character unit of measure.}

\item{observation_id}{Identifier of the related \code{\link[=ai_observation]{ai_observation()}}.}

\item{monitoring_id}{Identifier of the related \code{\link[=monitoring_spec]{monitoring_spec()}}.}

\item{note}{Optional character note recorded in the change log.}

\item{id}{Optional identifier. Auto-generated (with a type-specific prefix) if \code{NULL}.}

\item{owner}{Optional character owner identifier.}

\item{metadata}{Optional named list of free-form metadata.}

\item{provenance}{Optional named list recording where/how this record originated.}
}
\value{
A new \code{drift_assessment} object.
}
\description{
Deterministic and conservative. Without numeric baseline and observed
values, result is INCONCLUSIVE. No p-values or effect sizes.
}
\details{
With tolerance, \code{direction} controls which side of the baseline counts as
drift: \code{"MAINTAIN"} (default) flags any deviation whose absolute value
exceeds \code{tolerance}, \code{"UP"} flags only an increase beyond tolerance
(\code{observed - baseline > tolerance}), and \code{"DOWN"} flags only a decrease
beyond tolerance (\code{observed - baseline < -tolerance}); movement on the
other side is NO_DRIFT rather than treated as improvement or penalised.
Equal values are always NO_DRIFT. Without tolerance, unequal numeric
values are always INCONCLUSIVE regardless of \code{direction} (no invented
definition of meaningful change).
}
