% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/observation.R
\name{monitoring_spec}
\alias{monitoring_spec}
\title{Define what is being monitored for a system}
\usage{
monitoring_spec(
  id = NULL,
  system_id = NULL,
  name,
  metric,
  kind = "METRIC",
  operator = NULL,
  criterion = NULL,
  upper = NULL,
  unit = NULL,
  baseline_value = NULL,
  drift_tolerance = NULL,
  cadence = NULL,
  owner = NULL,
  metadata = list(),
  provenance = list()
)
}
\arguments{
\item{id}{Optional identifier. Auto-generated (with a type-specific prefix) if \code{NULL}.}

\item{system_id}{Identifier of the \code{\link[=ai_system]{ai_system()}} this record belongs to.}

\item{name}{Character name.}

\item{metric}{Metric name (character scalar), e.g. "AUROC" or "latency_p99".}

\item{kind}{Controlled kind/type value.}

\item{operator}{One of \link{CRITERION_OPERATOR_LEVELS}, or \code{NULL} for a purely
declarative spec with no executable criterion (evaluates to \code{INCONCLUSIVE}
via \code{\link[=evaluate_monitoring]{evaluate_monitoring()}} until both \code{operator} and \code{criterion} are set).}

\item{criterion}{Criterion value the observation is compared against. Leave
\code{NULL} (the default) together with \code{operator = NULL} for a declarative spec.}

\item{upper}{Optional numeric upper bound; required for the \code{BETWEEN}/\code{OUTSIDE} operators.}

\item{unit}{Optional character unit of measure.}

\item{baseline_value}{Optional numeric baseline value the monitoring spec tracks.}

\item{drift_tolerance}{Optional non-negative numeric drift tolerance.}

\item{cadence}{Optional character monitoring cadence, e.g. "daily".}

\item{owner}{Optional character owner identifier.}

\item{metadata}{Optional named list of free-form metadata.}

\item{provenance}{Optional named list recording where/how this record originated.}
}
\value{
A new \code{monitoring_spec} object.
}
\description{
A monitoring specification is declarative. It does not compute health
scores or invent thresholds beyond the explicit criterion supplied.
}
